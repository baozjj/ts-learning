# 基本类型

## 📝 基本类型详解

### 1. 原始类型

#### string（字符串）

```typescript
let name: string = "张三";
let greeting: string = "你好";
let message: string = `欢迎, ${name}!`; // 模板字符串

// ❌ 错误
// name = 123;
```

#### number（数字）

TypeScript 中所有数字都是浮点数。

```typescript
let age: number = 25;
let price: number = 99.99;
let hex: number = 0xf00d; // 十六进制
let binary: number = 0b1010; // 二进制
let octal: number = 0o744; // 八进制

// ❌ 错误
// age = "25";
```

#### boolean（布尔值）

```typescript
let isActive: boolean = true;
let isCompleted: boolean = false;

// ❌ 错误
// isActive = "true"; // 字符串不是布尔值
// isActive = 1;      // 数字不是布尔值
```

### 2. 数组（Array）

有两种定义数组的方式：

```typescript
// 方式一：类型 + []
let numbers: number[] = [1, 2, 3, 4, 5];
let names: string[] = ["张三", "李四", "王五"];

// 方式二：Array<类型>（泛型语法）
let scores: Array<number> = [90, 85, 88];
let fruits: Array<string> = ["苹果", "香蕉", "橙子"];

// 混合类型数组（不推荐，后面会学习更好的方式）
let mixed: (number | string)[] = [1, "two", 3, "four"];
```

### 3. 元组（Tuple）

元组是**固定长度**和**固定类型**的数组。

```typescript
// 定义一个元组：[string, number]
let user: [string, number] = ["张三", 25];

// 访问元素
console.log(user[0]); // "张三"
console.log(user[1]); // 25

// ❌ 错误的赋值
// user = [25, "张三"];        // 类型顺序错误
// user = ["张三", 25, true];  // 长度不匹配

// 实际使用场景：函数返回多个值
function getUser(): [string, number, boolean] {
  return ["张三", 25, true];
}

const [name, age, isActive] = getUser();
```

### 4. 枚举（Enum）

枚举用于定义一组命名的常量。

#### 数字枚举

```typescript
enum Direction {
  Up, // 0
  Down, // 1
  Left, // 2
  Right, // 3
}

let dir: Direction = Direction.Up;
console.log(dir); // 0

// 自定义起始值
enum Status {
  Pending = 1,
  Success = 2,
  Failed = 3,
}
```

#### 字符串枚举

```typescript
enum Color {
  Red = "RED",
  Green = "GREEN",
  Blue = "BLUE",
}

let color: Color = Color.Red;
console.log(color); // "RED"

// 实际使用场景：API 状态码
enum HttpStatus {
  OK = "200",
  NotFound = "404",
  ServerError = "500",
}

function handleResponse(status: HttpStatus) {
  if (status === HttpStatus.OK) {
    console.log("请求成功");
  }
}
```

#### 常量枚举（性能优化）

```typescript
const enum Size {
  Small = "S",
  Medium = "M",
  Large = "L",
}

let size: Size = Size.Medium;
// 编译后会被内联为 "M"，不会生成额外代码
```

### 5. any（任意类型）

`any` 类型可以是任何类型，**应该尽量避免使用**。

```typescript
let value: any = 123;
value = "hello";
value = true;
value = { name: "张三" };

// any 会关闭类型检查
value.foo.bar; // 不会报错，但运行时可能出错

// ⚠️ 使用场景：迁移 JavaScript 代码时的临时方案
```

### 6. unknown（未知类型）

`unknown` 是 `any` 的类型安全版本。
`unknown`类型可以被赋予任何类型的值，这一点和`any`类似。然而，它们之间存在一个至关重要的区别：

- `any`类型：当一个变量被指定为 any 类型后，TypeScript 编译器会放弃对该变量的所有类型检查。你可以对它进行任何操作，比如访问属性、调用方法等，即使这些操作在运行时可能会出错。
- `unknown` 类型：虽然一个变量可以是 `unknown` 类型，但在你对它执行任何操作之前，必须先进行类型检查或类型断言，来明确它的具体类型。这大大增强了代码的类型安全性。
  简单来说，`unknown` 强制开发者在处理不确定类型的数据时进行显式的类型验证，从而减少潜在的运行时错误。

```typescript
let value: unknown = "hello";

// ❌ 不能直接使用
// value.toUpperCase(); // 错误

// ✅ 需要先进行类型检查
if (typeof value === "string") {
  console.log(value.toUpperCase()); // 正确
}

// 实际使用场景：处理不确定类型的数据
function processValue(value: unknown) {
  if (typeof value === "string") {
    return value.toUpperCase();
  }
  if (typeof value === "number") {
    return value.toFixed(2);
  }
  return String(value);
}
```

### 7. void（空值）

`void` 表示没有返回值。

```typescript
// 函数没有返回值
function logMessage(message: string): void {
  console.log(message);
  // 不需要 return
}

// void 类型的变量只能赋值为 undefined 或 null
let unusable: void = undefined;
```

### 8. null 和 undefined

```typescript
let u: undefined = undefined;
let n: null = null;

// 默认情况下，null 和 undefined 是所有类型的子类型
let num: number = undefined; // 在非严格模式下可以

// 启用 strictNullChecks 后（推荐）
// let num: number = undefined; // ❌ 错误
```

### 9. never（永不存在的值）

`never` 表示永远不会有返回值的类型。
与 `void`的区别:
`void`：函数正常执行完了，但没有返回值。
`never`：函数永远无法正常执行完。

```typescript
// 总是抛出错误的函数
function throwError(message: string): never {
  throw new Error(message);
}

// 永远不会结束的函数
function infiniteLoop(): never {
  while (true) {
    // ...
  }
}

// 实际使用场景：详尽性检查
type Shape = "circle" | "square";

function getArea(shape: Shape): number {
  switch (shape) {
    case "circle":
      return Math.PI * 10 * 10;
    case "square":
      return 10 * 10;
    default:
      // 如果添加了新的 shape 类型但忘记处理，这里会报错
      const _exhaustiveCheck: never = shape;
      return _exhaustiveCheck;
  }
}
```

### 10. object（对象类型）

```typescript
// object 表示非原始类型
let obj: object = { name: "张三" };
obj = [1, 2, 3];
obj = function () {};

// ❌ 不能是原始类型
// obj = 123;
// obj = "hello";

// 更好的方式：使用具体的对象类型（后面会学习）
let user: { name: string; age: number } = {
  name: "张三",
  age: 25,
};
```
