# 变量声明与类型注解

## 🎯 为什么需要类型注解？

```typescript
// JavaScript - 类型不明确
let age = 25;
age = "twenty-five"; // 可以，但可能导致问题

// TypeScript - 类型明确
let age: number = 25;
age = "twenty-five"; // ❌ 编译错误
```

## 📝 变量声明

### 1. let 声明

`let` 用于声明可变的变量。

```typescript
let count: number = 0;
count = 10; // ✅ 可以重新赋值

let message: string;
message = "Hello"; // ✅ 可以先声明后赋值

// ❌ 不能重复声明
// let count: number = 20;
```

### 2. const 声明

`const` 用于声明常量，**必须在声明时初始化**。

```typescript
const PI: number = 3.14159;
// PI = 3.14; // ❌ 不能重新赋值

const userName: string = "张三";

// ⚠️ 注意：const 只保证引用不变，不保证值不变
const user = { name: "张三", age: 25 };
user.age = 26; // ✅ 可以修改对象属性
// user = { name: "李四", age: 30 }; // ❌ 不能重新赋值
```

### 3. var 声明（不推荐）

```typescript
var oldStyle: number = 10;
// var 有函数作用域和变量提升问题，应该避免使用
```

## 🏷️ 类型注解

### 1. 基本语法

```typescript
// 语法：变量名: 类型 = 值
let age: number = 25;
let name: string = "张三";
let isActive: boolean = true;
```

### 2. 类型注解 vs 类型推断

#### 类型推断（Type Inference）

TypeScript 可以自动推断变量的类型：

```typescript
// 类型推断 - TypeScript 自动推断为 number
let count = 0;
// count = "hello"; // ❌ 错误

// 类型推断 - 自动推断为 string
let message = "Hello";

// 类型推断 - 自动推断为 boolean
let isActive = true;
```

#### 何时需要类型注解？

```typescript
// 1. 声明时不初始化
let age: number;
age = 25;

// 2. 类型推断不准确时
let value: string | number = "hello";
value = 123; // ✅ 可以

// 3. 函数参数（必须）
function greet(name: string) {
  return `Hello, ${name}`;
}

// 4. 复杂类型
let user: { name: string; age: number } = {
  name: "张三",
  age: 25,
};
```

### 3. 联合类型

变量可以是多种类型之一：

```typescript
let value: string | number;
value = "hello"; // ✅
value = 123; // ✅
// value = true; // ❌ 错误

// 实际使用场景：ID 可以是字符串或数字
let userId: string | number;
userId = "user_123";
userId = 123;

// 数组中的联合类型
let mixed: (string | number)[] = [1, "two", 3, "four"];
```

### 4. 字面量类型

变量只能是特定的值：

```typescript
// 字符串字面量类型
let direction: "up" | "down" | "left" | "right";
direction = "up"; // ✅
direction = "down"; // ✅
// direction = "forward"; // ❌ 错误

// 数字字面量类型
let dice: 1 | 2 | 3 | 4 | 5 | 6;
dice = 3; // ✅
// dice = 7; // ❌ 错误

// 布尔字面量类型
let success: true = true;
// success = false; // ❌ 错误

// 实际使用场景：HTTP 方法
let method: "GET" | "POST" | "PUT" | "DELETE";
method = "GET";
```

### 5. 类型别名（Type Alias）

使用 `type` 关键字创建类型别名：

```typescript
// 定义类型别名
type ID = string | number;
type Direction = "up" | "down" | "left" | "right";

// 使用类型别名
let userId: ID = "user_123";
let direction: Direction = "up";

// 复杂类型别名
type User = {
  id: ID;
  name: string;
  age: number;
  email?: string; // 可选属性
};

let user: User = {
  id: 1,
  name: "张三",
  age: 25,
};
```

## 🎯 类型断言

有时你比 TypeScript 更了解某个值的类型，可以使用类型断言。

### 1. as 语法（推荐）

```typescript
let someValue: unknown = "this is a string";
let strLength: number = (someValue as string).length;

// DOM 操作中常用
const inputElement = document.querySelector("#username") as HTMLInputElement;
inputElement.value = "张三";
```

### 2. 尖括号语法（不推荐，与 JSX 冲突）

```typescript
let someValue: unknown = "this is a string";
let strLength: number = (<string>someValue).length;
```

### 3. 非空断言（!）

告诉 TypeScript 某个值一定不是 null 或 undefined：

```typescript
function processValue(value?: string) {
  // 我们确定 value 不是 undefined
  console.log(value!.toUpperCase());
}

// DOM 操作
const element = document.getElementById("app")!;
element.innerHTML = "Hello";
```

⚠️ **注意**：类型断言不会进行运行时检查，使用时要确保类型正确。
